# VPS Data Tools

Collection of lightweight utilities for managing data on a VPS.

## Features

* Import CSV files into PostgreSQL databases.

## Project Structure

```text
configs/
tools/
requirements.txt
README.md
```

## Configuration

Create a JSON configuration file inside `configs/` based on `template.json`.

## Requirements

```bash
pip install -r requirements.txt
```
